const Button = ({ styles }) => (
  <button
    type="button"
    className={`text-primary outline-none ${styles} bg-blue-gradient rounded-[10px] py-4 px-6 font-poppins text-[18px] font-medium`}
  >
    Get Started
  </button>
);

export default Button;
